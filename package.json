{
  "name": "grandhope",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-sitemaps.js",
    "build": "node scripts/sync-wp.js && npm run prebuild && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "node scripts/sync-wp.js",
    "sitemap": "node scripts/generate-sitemaps.js"
  },
  "dependencies": {
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "astro-purgecss": "^6.0.1"
  }
}
